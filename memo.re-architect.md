# やること

- oxcの導入
- vpの導入
- リリースコマンド、フローの整備
- コードをアーキテクトする
- ゲームカテゴリ作成コマンド追加

## config周りの機能追加

- 特定チャンネルに特定の形式でメッセージを書いてもらう
- パースして利用する
- 項目
  - vc通知先tc-id
  - シークレット通知先tc-id
  - シークレット通知対象vc-id

## アーキテクチャ

- feature/: 各機能を書く
  - 機能ごとにフォルダ分け
  - ロジックをかく
  - これを実行したらこの機能が実行されるみたいなのをexport
    - メッセージの送信とかもここに書く
  - client 全体ではなく「必要な送信関数だけ」渡す
- dispatch/: discordのイベントにfeatureの関数をディスパッチ
  - featureからimportしてきて、onXXXに関数をセット
  - readyもここに置く
  - settingチャンネルのアップデートも忘れない
- store/: 保持しておく値をここにおく
  - 設定などをインメモリで保持する

feature/にロジック(このチャンネルにこのメッセージを送る、この時はこのメッセージを送る)みたいなロジックをかく
onXXX.tsにロジックを書いてdiscordのイベント発火と機能の発火をマッチングする
onXXX.tsはfunctionとして定義しておいて、 main.tsで実行(= client.on()を実行)する

設定周りについては、store的な機能があるとよさそうだと思った。
onReady(?)で設定を読み出し、ストアへ保存、各featureではそのストアから設定を取得し利用するという形

こういう感じでどうだろうか
実送信については、featureにclient.send()とかを書いてしまう(どこかに腐敗防止層を設けてsend()になるかもしれないが、今回の議論には関係ないことだ)つもりなので、main.tsでonXXXを実行してclientにイベントをディスパッチ、それが発火したらfeatureの関数が発火し、メッセージの送信などがfeature関数内部の処理として実行、それによりメッセージが送られるという感じになると思う。
